import pandas as pd
# from pathlib import Path
# import scipy.io


def load_raw(path: str) -> pd.DataFrame:
    """
    Stub für das Einlesen der rohen DS02-Daten.
    Liest alle CSV- und MAT-Dateien unter dem angegebenen Pfad
    und liefert einen zusammengefügten DataFrame zurück.
    """
    # TODO: hier kommt später die echte Logik zum Einlesen von CSV- und MAT-Dateien
    # vorläufig geben wir einen leeren DataFrame zurück, damit mypy und Tests nicht meckern
    return pd.DataFrame()
